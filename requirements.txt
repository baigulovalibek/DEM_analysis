PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0
PyOpenGL>=3.1.7
numpy>=1.26.0
scipy>=1.11.0
rasterio>=1.3.0
matplotlib>=3.8.0
pyqtgraph>=0.13.0
Pillow>=10.0.0
pandas>=2.0.0
openpyxl>=3.1.0
# Optional: install Numba for 20-80x speedups on the hydrology + viewshed
# kernels (see OPTIMIZATION.md Phase B).  The app runs correctly without
# it; the pure-Python fallbacks are just slower.
# numba>=0.59
